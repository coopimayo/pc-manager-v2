{
  "name": "pc-manager-v2",
  "private": true,
  "version": "0.0.0",
  "description": "D&D player-character manager: data-driven rules engine + content packs",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
