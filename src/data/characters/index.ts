import type { Character } from '../../types';
import { exampleFighter } from './example-fighter';
import { veraQuickblade } from './vera-quickblade';

export { exampleFighter, veraQuickblade };

export const characters: Character[] = [exampleFighter, veraQuickblade];
